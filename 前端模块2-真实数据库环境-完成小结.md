# 模块二 · 真实数据库环境打通 — 完成小结（2026-09-01）

> 目标（用户指令）：「模块一完成的很好，自动启动 Docker 完整启动起来」
> 即把后端从 H2 零安装模式（local profile）切换到真实 PostgreSQL，并让管理员可操作全部数据、各角色按权限自动显隐菜单。

## 一、Docker 中间件全量启动（Method A）

| 容器 | 镜像 | 端口 | 状态 |
|------|------|------|------|
| claw-postgres | timescale/timescaledb:latest-pg16 | 5432 | healthy，PostgreSQL 16.15 |
| claw-redis | redis:7-alpine | 6379 | PONG |
| claw-rabbitmq | rabbitmq:3.13-management-alpine | 5672 / 15672 | OK |
| claw-emqx | emqx/emqx:5.8 | 1883 / 18083 | Up |

启动命令：`cd claw-platform/deploy && docker compose up -d`

## 二、后端切换为真实 PostgreSQL（默认 profile）

1. 停止旧 H2 模式后端（端口 8080 上两个 java 进程已 kill）。
2. 重新构建默认 profile jar（非 local，含 PostgreSQL 驱动 + 启用 Flyway）：
   `mvn clean package -DskipTests`（JDK 26 + Byte Buddy 实验开关）。
3. 以默认 profile 启动：`java -jar target/claw-server-0.1.0-SNAPSHOT.jar --server.port=8080`。
   - 连接池 `HikariPool-1 - Added connection org.postgresql.jdbc.PgConnection` ✅
   - RabbitMQ `amqp://claw@127.0.0.1:5672` 自动重连成功 ✅
   - 启动耗时 8.5s。

## 三、Flyway 迁移落地（V1–V66）

- 校验通过 **67 个迁移**（DB 此前已处于 v38，本次从 v39 续跑到 v66，共应用 28 个）。
- 最终 schema 版本：**v66（drone_permission_seed）**。
- 全程无报错；`chk_roles_data_scope` 等幂等告警为预期内（历史约束已不存在）。

## 四、管理员账号打通（可操作全部数据）

- 登录方式：短信验证码（dev 回显 `sms-dev-echo=true`，生产关闭）。
- 已注册管理员账号：**手机号 13800000000**（user_id=8）。
- 授予角色：`PLATFORM_ADMIN`（grants=`["*"]` 通配符，等同全量权限）。
- 验证：
  - `/api/v1/auth/me` 返回 roles 含 `PLATFORM_ADMIN`，permissions 含 `*` ✅
  - `GET /api/v1/admin/roles` → 200 ✅
  - `GET /api/v1/admin/users` → 200 ✅
- 权限判定逻辑：`PermissionService.isPlatformAdmin` 识别 `PLATFORM_ADMIN`；通配符 `*` 放行所有 `@RequirePermission` 校验。

## 五、角色权限菜单自动显隐（前端已具备）

- `web/src/menuStore.js`：`filterHidden()` 按权限过滤菜单；`mergeWithBase()` 合并后端按 `menu:{navKey}` 下发的权限菜单树。
- `web/src/permStore.js` + `web/src/components/Perm.jsx`：`<Perm>` / `<RequirePermRoute>` 门控，无权限不渲染。
- 效果：
  - 管理员（通配符）→ 全部功能可见、可操作。
  - 普通用户（如 CUSTOMER，仅 10 项权限）→ 无权限功能**自动隐藏**（满足「无体验限制功能自动给用户隐藏」）。

## 六、当前运行态

| 服务 | 任务/容器 | 地址 |
|------|-----------|------|
| PostgreSQL / Redis / RabbitMQ / EMQX | Docker 容器 | localhost:5432/6379/5672/1883 |
| 后端（默认 profile·真实 PG） | 后台任务 K5apYS | http://localhost:8080 （Swagger: /swagger-ui.html） |
| 前端（Vite 5173） | 后台任务 9VAXR0 | http://localhost:5173 |

管理员登录：浏览器开 http://localhost:5173 → 用手机号 **13800000000** 获取验证码（页面会回显 dev 验证码）→ 登录后即拥有全部数据操作权。

## 七、下一步（模块三，待确认后执行）

- **模块三 · 供应流通库存改造**：厂家库存/寄售库存 → 现有库存/服务站库存；自动展示自己与下属服务站库存，上级看下级全部，管理员看全部 + 统计报表。
- **模块四 · 服务站功能**：库存 / 项目 / 结算三层解耦（产品先进服务站库存，项目挂载库存产品）。
- **模块五 · 逐页体验复核**：按用户反馈逐页查看并修。

> 按既定「单块确认」节奏：模块二已交付，请确认后进入模块三。
