# Phase 1 副本 pg_hba（原型）
# 副本以只读 standby 运行，但也接受连接（提升后变为可写主）。
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust
host    all             all             0.0.0.0/0               scram-sha-256
host    replication     all             0.0.0.0/0               scram-sha-256
