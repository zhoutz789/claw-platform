# Claw 平台 · 启动验证与完备性报告（2026-09-01）

## 一、结论先行

- **代码层面完备**：主代码 275 文件编译通过、单测 69 个历史通过、57 个 REST Controller、66 个 Flyway 迁移脚本（V1–V66）、管理后台前端 `web/` 已落地。
- **启动验证通过**：本次成功把后端跑起来（零安装 H2 模式）+ 前端管理后台联调就绪，**两端当前均在运行**。
- **本次修复 1 个真实缺陷**：`local` 零安装模式因业务代码硬依赖 `StringRedisTemplate` 导致**启动即失败**；已改为可选注入、缺失时优雅降级直查 DB，零安装 H2 模式从此真正可用。
- **已知边界（非缺陷）**：当前跑的是 H2 内存库（数据重启即空），非生产 Docker/PG 全栈；生产模式需启动 Docker 中间件（PG/Redis/RabbitMQ/EMQX），本机 Docker 当前未运行。

## 二、启动方式与运行态

| 组件 | 地址 | 状态 | 说明 |
|------|------|------|------|
| 后端 API（H2 零安装） | http://localhost:8080 | ✅ UP | `/actuator/health` 返回 200，`db=H2` |
| Swagger UI | http://localhost:8080/swagger-ui.html | ✅ 302→可用 | OpenAPI `/v3/api-docs` 返回 200 |
| 前端管理后台（Vite） | http://localhost:5173 | ✅ 200 | 代理 `/api → :8080` 联调 |

> 后端采用 `application-local.yml`：嵌入式 H2（PG 兼容模式）、关闭 Flyway、排除 Redis/RabbitMQ 自动配置，无需 Docker 即可冒烟。

## 三、启动中暴露并修复的缺陷

**现象**：`APPLICATION FAILED TO START` —— `No qualifying bean of type 'org.springframework.data.redis.core.StringRedisTemplate'`。
**根因**：`PrincipalResolver`、`PermissionService` 用 `@RequiredArgsConstructor` 将 `StringRedisTemplate` 声明为 `final` 构造器参数；而 `local` profile 故意排除了 Redis 自动配置。虽业务逻辑已写 `if (redisTemplate == null)` 降级，但**构造器注入在容器启动时就强制要求 Bean 存在**，于是启动直接失败。

**修复（最小侵入、零新依赖、符合设计者"Redis 可选降级"意图）**：
1. `PrincipalResolver.java` / `PermissionService.java`：字段由 `final` 构造器注入 → `private StringRedisTemplate redisTemplate;` + `@Autowired(required = false)` 字段注入；代码内既有 `null` 降级逻辑自然生效。
2. `PermissionServiceTest.java`：`newService()` 去掉 `redisTemplate` 构造参数，改用 `ReflectionTestUtils.setField(...)` 注入 mock，保留"Redis 降级 / 缓存命中 / evict"全部验证。

**修复后**：后端在完全无 Redis 环境下正常启动，权限/主体解析自动降级直查 DB，功能不受影响。

## 四、冒烟结果（同命令内实测）

```
actuator/health   200   v3/api-docs    200
swagger-ui.html   302   ping           403  (受保护)
countries         403   api/v1/admin/users 403  (Spring Security 鉴权生效)
```
后端进程经平台后台任务托管，已确认**跨命令持续存活**（多次独立 curl 均正常）。

## 五、构建与运行命令（备查）

```bash
# 后端（local 零安装模式，需 JDK 21 + local profile 才能把 H2 打进 jar）
export JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home
cd claw-platform/backend
mvn -Plocal clean package -DskipTests
java -jar target/claw-server-0.1.0-SNAPSHOT.jar --spring.profiles.active=local --server.port=8080

# 前端
cd claw-platform/web && npm run dev   # http://localhost:5173
```

> ⚠️ 教训：jar 必须用 `-Plocal` 构建，否则 H2 驱动不在 classpath；且 `mvn package` 在"源码未变"时会跳过 jar 重写，改代码后务必 `clean package`。

## 六、后续建议

1. **全量单测回归**：建议在 Docker 就绪时跑一次 `mvn test`（含 Testcontainers/PG 集成测试），确认本次改动未影响其它用例。
2. **生产模式联调**：启动 `deploy/docker-compose.yml` 后，用默认 profile 连真实 PG/Redis/RabbitMQ/EMQX 验证全链路。
3. **前端登录**：dev 模式 `sms-dev-echo=true`，点"获取验证码"直接回显，默认手机号 `13800000001`。
