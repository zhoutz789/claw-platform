# Claw 平台 · 分布式服务中心部署架构建议

> 背景：周老板提出"在不同服务站建立分布式服务中心，数据总平台集中管理，可像在一台服务器上
> 调取各站数据；默认把数据备份到不同区域服务站，一旦某节点损毁，业务照常运营、并迅速再备份到别的服务器"。
> 本文给出落地路线，不与现有单主机部署冲突，而是其演进形态。

---

## 0. 结论先行

你想要的"像在一台服务器上 + 损毁不断服 + 自动再备份"，本质是 **PostgreSQL 的跨区流式复制 + 持续 WAL 备份 + 自动故障转移/重播**。分三阶段演进，**不要一步到位上 K8s 多活**：

1. **Phase 0（已具备）**：单机 Docker Compose + 版本切换（`deploy-version.sh`）。
2. **Phase 1（最优先）**：数据库换成**托管 PostgreSQL/TimescaleDB + 跨区只读副本 + 对象存储持续备份（PITR）**。这一步就满足了"默认备份到不同区域"和"主节点损毁自动切换、数据零丢失"。
3. **Phase 2（多站规模化）**：每个区域枢纽跑**边缘节点**（本地 PG 自治 + 复制到中心 + 互备），中心用逻辑复制/CDC 聚合出"统一视图"，实现"像一台服务器"。
4. **Phase 3（终极形态，可选）**：核心交易库改用 **CockroachDB / YugabyteDB** 这类分布式 SQL——它们原生"区域损毁自动重分布、自动再副本"，正是你描述的"迅速再被备份到不同服务器"的开箱能力；时序遥测仍用 TimescaleDB（按区域落地再聚合）。

---

## 1. 你的四条诉求 → 架构映射

| 诉求 | 落地机制 |
|------|---------|
| ① 不同服务站建分布式服务中心 | 每个区域枢纽 = 一个独立部署单元（边缘节点）：本地 PG + 后端 + 网关，套用现有 `deploy/docker-compose.prod.yml` + `CLAW_VERSION` 版本切换 |
| ② 总平台集中管理 | 中心控制面：聚合各节点元数据的**中心只读库**（逻辑复制/CDC 入中心），管理后台走中心；业务写入仍归各节点或中心主库 |
| ③ 像在一台服务器上查各站数据 | 中心**统一查询层**：应用层按 `station_id` 聚合（claw 现有 `InventoryScope` 已天然分域）；物理上用中心只读副本 / 联邦查询提供单视图 |
| ④ 默认备份不同区域 + 损毁不断服 + 迅速再备份 | **N 副本复制**（每节点数据默认复制到 ≥2 个其他区域）+ **WAL 持续备份到对象存储并跨区拷贝**；节点损毁 → 副本提升/重定向，数据完整 → 业务继续 → 系统自动把丢失节点的数据重新播种到新/备用节点 |

---

## 2. 推荐拓扑（Phase 1，最务实）

```
                 ┌──────────── 对象存储（OSS/S3），WAL 持续归档 ────────────┐
                 │   跨区拷贝（如 金边→新加坡→曼谷），默认异地备份          │
                 └───────────────────────┬───────────────────────────────┘
                                          │ 持续 WAL 归档 / 恢复
  区域A(金边中心)                区域B(暹粒枢纽)                  区域C(西港枢纽)
  ┌─────────────────┐          ┌─────────────────┐           ┌─────────────────┐
  │ Primary PG       │◀─stream─▶│ Replica PG       │           │ Replica PG       │
  │ (TimescaleDB)    │  async   │ (读/灾备)        │  async   │ (读/灾备)        │
  │ backend/web/gw   │          │ backend/web/gw   │           │ backend/web/gw   │
  └─────────────────┘          └─────────────────┘           └─────────────────┘
   写入主库；B/C 实时复制        任意节点损毁→提升副本          业务零中断
```

- **写入**：核心交易（账户/订单/库存）走中心 Primary；有强一致要求。
- **读取/灾备**：B、C 为流式副本，可承接只读查询（降低中心延迟），并在 A 损毁时**秒级提升**为新的主。
- **备份**：WAL 实时归档到对象存储并跨区拷贝——这就是"默认备份到不同区域"。即使 A+B 同时毁，C + 对象存储仍可恢复（RPO≈0，RTO 分钟级）。

---

## 3. Phase 2/3：边缘自治 + 统一视图（规模化后）

- **边缘节点**：每个枢纽本地 PG 自治，断网也能做换电/取货等本地业务（写入本地），恢复后增量同步到中心与互备节点。
- **中心聚合**：用 **逻辑复制 / Debezium CDC** 把各节点变更汇入中心只读库，管理后台在此做"全平台一张图"——实现诉求③。
- **终极形态**：核心库换 **CockroachDB/YugabyteDB**——`REPLICATION = 3 区域`，任一区域宕机，数据自动在存活区域**重新均衡再副本**，业务无感。这正是诉求④的"开箱"实现，但需放弃 TimescaleDB（时序遥测另走 TimescaleDB 按区域落地 + 聚合）。

---

## 4. 技术选型（映射到 claw 栈）

| 能力 | 推荐 | 说明 |
|------|------|------|
| 复制 | PostgreSQL 流式复制（物理） | 原生、零额外组件；副本只读 |
| 自动故障转移 | Patroni + etcd **或** 云托管（RDS/Azure/云厂商 PG 高可用） | 托管最省心，推荐起步 |
| 持续备份 | pgBackRest / WAL-G → 对象存储 | WAL 归档 + 跨区拷贝 = 异地备份 |
| 统一查询 | 中心只读副本 + 应用 `station_id` 聚合（现有 `InventoryScope`） | 不改业务模型 |
| 边缘 CDC | Debezium / 现有 Outbox 模式 | claw 已有 `outbox_events` + `OutboxRelay`，可复用入中心 |
| 编排演进 | Compose → K8s + CloudNativePG 算子 | 多区域后再上，MVP 不急 |
| 分布式 SQL（可选）| CockroachDB / YugabyteDB | 终极多活，替代 PG 主库 |

---

## 5. 两个关键坑（务必提前规避）

1. **Flyway 不能在副本上跑**：副本是只读的，后端若带 Flyway 启动会失败。
   → 迁移只在 **Primary** 执行（用一个 `--migrate-only` 的一次性 Job，或中心节点专门跑 Flyway）；副本经复制获得 schema。
   逻辑复制场景更要小心 DDL 传播。
2. **CAP 取舍**：跨区异步复制下，网络分区时要么**保一致（停写）**要么**保可用（可能读到旧值）**。
   → 核心资金链路用**同步副本（至少 1 个）**保证零丢失；非核心遥测可用异步。资金"绝对准确"红线优先于可用性。

---

## 6. 与已做"版本切换"的结合

现有 `scripts/deploy-version.sh` 的 `CLAW_VERSION` 机制**每个节点通用**：
- 中心与各边缘节点各自持 `deploy/.env.prod`，改 `CLAW_VERSION` + `deploy` 即可逐节点换版。
- 数据库层不随应用版本切换；Flyway 前向迁移由中心 Primary 统一执行，副本跟随。
- 回滚前先备 DB 快照的规则（已在方案文档写明）在多节点下更关键：升级前对每个区域主库做一次快照。

---

## 7. 下一步建议

1. **立刻可做（Phase 1）**：把单机 PG 换成托管高可用 PG + 开跨区只读副本 + 配 WAL 异地归档。这是性价比最高的一步，直接满足②③④。
2. **验证**：用我们刚跑通的单机环境做"主库宕机→副本提升→业务继续"的演练脚本。
3. **正式设计文档**：若要从 Phase 1 走到 Phase 2/3，可由架构师出一份含拓扑图、迁移策略、灾备演练 Runbook 的详细设计（含 Terraform / 云厂商具体配置）。
4. **资金红线**：任何跨节点复制方案，核心账户/账本必须同步副本或强校验，绝不允许异步导致余额口径漂移。
