# 设计文档审阅报告（2026-09-05）

> 审阅人：团队负责人（基于架构师「高见远」、工程师交付与代码实测）
> 审阅对象：`claw-platform/` 下 7 份未提交设计/小结文档
> 审阅方式：文档通读 + 代码实测对照（非仅摘要复述）

## 0. 结论速览

| 维度 | 结论 |
|---|---|
| 设计完整度 | 模块三/四/五 三套增量设计文档**齐备、自洽、证据化（file:line）**，质量高 |
| 代码落地 | 模块三 ✅已实现+小结；模块四 ✅已全栈实现（含 V68 迁移）；模块五 ✅部分实现（T-UX1/2/5/6 落地） |
| 文档缺口 | 7 份中**缺《模块四·完成小结》《模块五·完成小结》**两份归档 |
| V68 状态 | 离线迁移链绿灯（68 脚本连续）；全量 *IT 待本机 Docker 跑 |

7 份文档均位于 `claw-platform/`（独立 git 仓库，当前未提交）。

---

## 1. 逐文档审阅

### 1.1 增量设计-模块三-库存改造.md（717 行）
- **内容**：需求锚点 M3-1~M3-4、技术选型（零新框架/零新表）、4 大难点决策、视图口径、权限安全、文件清单、接口契约、类图/时序图、任务 T01–T05、依赖包、跨文件共享约定、H1–H9 拍板项。
- **质量**：极高。架构师「高见远」出品，论证充分（零新表/零新依赖/不引入图表库均有先例支撑）。
- **一致性**：H1–H9 用户已采用架构师默认拍板；与《模块三-库存改造-完成小结.md》完全对齐（25/25 测试绿、V67 迁移落地、四角色行为一致）。
- **遗留债（重要）**：H-9 指出 `PrincipalResolver.resolveByType` 既有缺陷——一个既绑定厂家又绑定服务站的账号，`resolveByType(uid, STATION)` 会误返 `empty`（因 `resolve()` 按 MANUFACTURER→STATION→MERCHANT 只取首个）。模块三用新增 `resolveAll` 绕过、**未修根因**。此债影响 `OrgWritableGuard.assertApplicantCanApply` 入驻链路判定 → 建议单列回归项（当前无人跟）。

### 1.2 增量设计-模块四-服务站三层解耦.md（465 行）
- **内容**：D1–D12 决策、Mermaid 架构/时序图、数据模型（5 新表）、三层后端文件清单、端点表、前端三页、权限矩阵、BC-1~BC-6 解耦边界检查点、T-INFRA~T-QA 任务、开放问题默认决议、编译自检清单。
- **质量**：极高。「写事务各自封闭 / 跨层仅 ID 引用 / 分配不扣库存」设计清晰，BC 检查点可直接转验收用例。
- **代码对照（本次实测）**：
  - `V68__station_module_three_layer.sql` 存在，**5 张 `CREATE TABLE`**（station_inventory_movements / station_projects / station_project_inventory_alloc / station_settlements / station_settlement_items）✅
  - 后端 7 文件齐全：StationInventory/Project/Settlement ×{Controller,Service} + StationScopeService ✅
  - 前端三页 + `station.json` + `api/station.js` 齐全 ✅
  - → **模块四已全栈实现**，与设计并进一致。
- **缺口**：本批 7 份文档中**缺《模块四·完成小结》**——实现已落地但无验收小结归档，建议补一份（仿模块三格式：测试绿/解耦断言/红线 `git diff` 空）。

### 1.3 增量设计-模块五-逐页体验复核.md（305 行）
- **内容**：81 页全量评分卡（79 路由页+1 共享+1 死代码）、分组聚合、Top18 硬编码中文页、P0/P1/P2 分级、T-UX1~T-UX6 任务、架构师默认决议、验收建议。
- **质量**：高。静态扫描证据充分（file:line），评分卡可量化（均值 5.67/9，新页 7–9、存量页 4–5）。
- **代码对照（本次实测）**：
  - **T-UX1（P0 任务发布 4 子页补侧栏）**：✅ 已落地 — `nav.js` L104–107（`task-logi/ad/video/near`）+ ROUTES L237。
  - **T-UX2（26 页 en/km 译文 m1..m1003）**：✅ 已提交 `d08cf08`（SE-2/SE-5 双核验：1003 键、0 空值、0 与 zh 同值）。
  - **T-UX5/T-UX6（ScopeBanner/空态/响应式/product-publish 导航）**：✅ 已提交 `dabd607`（11 文件，build 0 错）。
  - **T-UX3（语言包全量 diff 补齐，含 km/supply.json `transfers.msg.handonvered` 拼写修正）**：⏳ 未完成（P1 收尾，建议单列小增量）。
  - **T-UX4（路由级权限门控）**：⏸ 周老板拍板**暂缓**（fail-open 保留，不动后端/模块四解耦，独立排期）。
- **缺口**：本批 7 份文档中**缺《模块五·完成小结》**。

### 1.4 三份过程性归档（已落地模块）
- **前端模块1-滚动与菜单修复-完成小结.md**（41 行）：布局滚动锁死 + 旧菜单清理，build 绿，待浏览器确认。
- **前端模块2-真实数据库环境-完成小结.md**（68 行）：Docker 全量启动 + 切真实 PG + V1–V66 迁移 + 管理员账号打通 + 菜单显隐。
- **启动验证与完备性报告-2026-09-01.md**（59 行）：H2 零安装启动 + 修复 `StringRedisTemplate` 可选注入缺陷 + 冒烟。
- **评价**：内容真实、命令可复现、与代码无矛盾。

---

## 2. 跨文档一致性 / 矛盾点

1. **模块三 H-9 的 `resolveByType` 缺陷债** → 与入驻域 / `OrgWritableGuard` 强相关，当前无任务跟，建议登记独立回归项。
2. **模块四 V68 迁移号**与设计一致（当前最大 V67 之后 → V68）；离线核验 V1..V68 连续 ✅。
3. **模块五评分卡**「均值 5.67/9」「模块四 3 页 9/9、模块三新页 7–9」规律自洽，新页面是存量页改造样板。
4. **文档闭环缺口**：模块四/五缺「完成小结」，建议补齐使三模块「设计-实现-小结」闭环。

---

## 3. V68 验证状态（本机 vs 沙箱）

- **本 agent 沙箱**：Docker daemon 被杀（任何 `docker` 调用 SIGTERM 137）、PG `5432` 当前 **DOWN** → 无法跑 `*IT`。
- **离线检查（已实测）**：`./verify_v68.sh offline` → **68 脚本、V1..V68 连续 ✅**（对应 `SchemaMigrationIT.countMigrationScripts()` 期望值）。
- **全量闭环命令（需在周老板 macOS + Docker Desktop 运行）**：
  ```bash
  cd claw-platform && ./verify_v68.sh
  ```
  脚本自动：起栈（docker compose）→ 建 `claw_it` 库 → 跑 5 个 `*IT`（SchemaMigrationIT / AdminInventoryControllerMeStatsIT / ConsignmentCustodyIT / LedgerDoubleEntryIT / ProjectLedgerIT）→ 日志落 `/tmp/v68_it.log`。
  **跑完把 `/tmp/v68_it.log` 发我，我做通过性判定。**

---

## 4. 关于这 7 份文档的「入库」建议

- **现状**：7 份均为 `claw-platform` 仓库未提交文件（记忆中标注「故意不提交待审阅」）。
- **建议**：审阅通过后**提交进 `claw-platform` 仓库**（它们是与代码同生命周期的设计/验收资产，比根仓库更适合归档）。
- **待办**：先补《模块四·完成小结》《模块五·完成小结》两份归档，再 7 份统一提交，使三模块设计-实现-小结闭环。

---

## 5. 下一步建议（按优先级）

1. 【你本机】跑 V68 全量：`./verify_v68.sh` + 发 `/tmp/v68_it.log`。
2. 【小增量】T-UX3 语言包全量 diff + km 拼写修正（收尾模块五 P1）。
3. 【债】登记 `resolveByType` 多绑定缺陷回归项（H-9 遗留）。
4. 【文档】补模块四/五完成小结，7 份文档统一提交 `claw-platform`。
5. 【暂缓·独立排期】T-UX4 路由级权限门控（架构师标较高风险）。
