# 模块三 · 供应流通库存改造 — 完成小结（2026-09-02）

> 目标（用户指令）：「进入模块三（库存改造）」= 供应流通库存改造：
> 厂家库存/寄售库存 → **现有库存 / 服务站库存**；自动展示自己与下属服务站库存；上级看下级全部；管理员看全部 + 统计报表。

## 一、设计结论（架构师高见远）
- 完整设计文档：`增量设计-模块三-库存改造.md`（717 行，A–H 全章节 + 类图/时序图/任务依赖图）。
- **零新表、零新依赖、零新框架**：仅新增一支 `V67` 幂等菜单权限种子（纯 INSERT，无 DDL）。
- 两条写链路（发货/寄售/授信）**一行不动**。

## 二、实现要点（工程师寇豆码，IS_PASS=YES）
**后端**
- 新增 `InventoryScope` / `InventoryScopeService`：主体解析（`PrincipalResolver.resolveAll` 仅追加，不改既有 `resolveByType`）→ 作用域 → 越权校验 → 下属站动态反查。
- 新增 `InventoryViews`（安全 DTO，剔除 `unitValue/valueCurrency/unitValueSource`）、`InventoryStatsQuery`（JdbcClient 只读聚合）。
- `AdminInventoryController` 新增 `GET /me`、`GET /stats`；既有 `GET /` 与 `/device/{id}` 一并收口为安全 DTO（H-7）。
- `V67__inventory_overview_menu_seed.sql`：库存总览菜单 + 权限种子（sort_no=450，grants 回写严格限于 MANUFACTURER/STATION，不动 CUSTOMER）。

**前端**
- 新增 `InventoryOverview.jsx`（三 Tab：现有库存 / 服务站库存 / 统计报表）+ `inventoryShared.jsx`（共享列工厂 + ScopeBanner）。
- `MfgInventory`/`StationConsignment` 改为默认按当前主体自动作用域（保留手选增强）。
- `nav.js`/`App.jsx` 把「库存总览」置为供应流通组**首位默认入口**；三语（zh/en/km）supply.json + nav.json 同步补完。

## 三、验证结果
| 项 | 结果 |
|---|---|
| 前端 `npm run build` | ✓ 零错误（3283 modules） |
| 后端 `mvn compile`（JDK21） | ✓ BUILD SUCCESS |
| 单元测试 `InventoryScopeServiceTest` | 11/11 通过 |
| 端到端 `AdminInventoryControllerMeStatsIT`（真库 localhost:5432/claw_it） | 11/11 通过 |
| 写链路回归 `ConsignmentCustodyIT` | 3/3 通过 |
| **合计** | **25/25 全绿** |

**QA 修复的源码缺陷**：`InventoryStatsView` 漏传设计要求的 `byOwnership` 维度（`InventoryViews.java:116` 补字段、`InventoryService.java:241` 补传 `byOwn`）——最小改动、向后兼容，前端忽略多余字段无影响。

**四条高压安全规则（逐条 PASS）**
1. 敏感货值字段（`unitValue` 等）绝不下发 —— 对全部 `/me`、`/stats` 响应做 JSON key 递归断言，零命中。
2. 越权覆盖 `manufacturerId/stationId` 失败关闭，抛 `40301 inventory.scope.forbidden`（非 200 返回他人数据）。
3. `scopeLevel=NONE`（未绑定主体）返回空 + 引导文案，**不抛 403**（避免 dev 联调白屏）。
4. 下属服务站动态反查：仅返回"我的货寄在哪些站"，不依赖任何新表。

## 四、四角色行为摘要
- **厂家**：现有库存=自有(OWNED_BY_MFG，不含寄售)；服务站库存=寄售在站按 holderStationId 分组；统计 Tab 不可见。
- **服务站**：现有库存=本站在库 CONSIGNED；服务站库存 Tab 隐藏；统计不可见。
- **平台管理员**：现有库存=全平台 OWNED_BY_MFG；服务站库存=全平台 CONSIGNED 按站分组（展开懒加载明细）；统计报表可见（总数/自有/寄售/在厂/寄售占比/状态分布/站点 Top50/厂家分布/所有权分布）。
- **未绑定**：解析为空作用域，不抛 403，前端引导「请先绑定主体」，双视图与统计为空/不可见，不白屏。

## 五、锁定决策（用户跳过逐项提问，采用架构师推荐默认）
- H-1：新增「库存总览」作默认入口，旧两页保留为高级手选视图。
- H-2：厂家现有库存仅含 OWNED_BY_MFG（防双计）。
- H-3：下属服务站动态反查，不建表。
- H-7：既有 `/admin/inventory` 一并收口为 DTO。

## 六、当前运行态（浏览器验收）
| 服务 | 地址 |
|---|---|
| 后端（真实 PG，默认 profile） | http://localhost:8080 |
| 前端（Vite 5173） | http://localhost:5173 |

**验收步骤**：浏览器开 http://localhost:5173 → 用管理员手机号 **13800000000** 获取验证码（dev 回显）→ 登录 → 供应流通组首位点「库存总览」：
1. 管理员可见「现有库存 / 服务站库存 / 统计报表」三 Tab，统计报表 7+1 维度齐全；
2. 切厂家视角（绑定厂家账号）应只见自有库存 + 寄售在站分组；
3. 切服务站视角只见本站在库寄售，且「服务站库存」Tab 隐藏。

## 七、下一步（待确认后执行）
- **模块四 · 服务站功能**：库存 / 项目 / 结算三层解耦（产品先进服务站库存，项目挂载库存产品）。
- **模块五 · 逐页体验复核**：按用户反馈逐页查看并修。
- 备注：模块三仅做"视图与作用域"，未触碰货权/占有权产生与转移、授信校验、入驻守卫、服务站功能（模块四）、逐页复核（模块五）。
