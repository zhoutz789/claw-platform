# 爪平台 · 后台管理菜单操作手册

> 侧边栏真实导航由 `src/nav.js` 的 `NAV` 驱动（五大中心）。本文按此结构逐菜单说明**用途 + 操作方式**。
> 顶部若有黄色「⚠️ 演示数据模式」提示条，表示当前展示的是内置演示数据（闭环可见），接入真实后端并登录后自动切换为实时数据。

## 一、工作台（workbench）
- **用途**：平台经营总览——站点数、资产数、订单/收益汇总、待办。
- **操作**：进入即看；可按时间/中心筛选卡片。

## 二、运营中心（ops）
### 订单管理
- **订单总览（orders）**：全平台订单列表。操作：顶部按状态/日期筛选 → 点行看详情。
- **换电记录（swap-orders）**：换电订单流水。操作：列表查看 + 导出。

### 站点管理（stations）
- **用途**：服务站/换电站台账（金边各站）。
- **操作**：新增/编辑站点、查看站内资产与站长。

### 资产管理
- **资产台账（assets）**：全部资产（车/电池/无人机）列表与状态。操作：筛选、查看资产档案。
- **资产溯源（asset-trace）**：单资产全生命周期轨迹（建档→运营→维修→转让→退役）。操作：选资产编号看时间线 + 收益说明。
- **产权链追溯（custody）**：所有权转移链（三专户托管记录）。操作：查某资产历次转让。

### 厂家与商品
- **厂家商品（manufacturer）**：品牌方及其商品。操作：查看品牌资质/商标验证、商品。
- **品牌方入驻（brand-onboarding）**：新品牌方入驻申请/审核。操作：填写资料 → 提交 → 审核。

### 物联网设备（核心建模区）
- **设备物联（product-iot）** — 4 个 tab：
  - **产品模板**：平台级「类」定义（电动两轮车 / 共享电池 / 植保无人机），含可自定义字段。**现已可点「新增字段」扩展建模**（类型：数字/文本/单选/日期/布尔 + 单位 + 示例），改动持久化本地，并自动联动到派生品牌产品与设备概览。
  - **品牌产品**：品牌方基于模板创建的实例，展示规格 spec + 设备数；点卡片看名下设备。
  - **设备**：每台具体对象；点「数字孪生详情」看 定位/赋权/收益/维修/转让；无人机额外有「低空·载荷」（空域合规 / 飞行计划 / 作业计量 / 生命周期闭环）。
  - **合格证**：设备合格证 + 二维码验真（防套牌）。
- **资产参数（asset-params）**：产权现值核算参数（年利率 / 计息方式 / 按资产类型差异化）。操作：填表单 → 保存（演示）。

### 共享池（shared-pool）
- **用途**：资产共享池入池/出池、共享调度。操作：列表 + 调度动作。

### 残值回收（recovery）
- **用途**：退役资产回收与残值核算（固定 30% 残值归资产所有人）。操作：发起回收 → 核算残值。

### 任务调度（task-publish）
- **资产出租/共享**：选资产 + 日租金 + 权限模板发布。
- **无人机任务**：植保/巡检/救援任务发布 + 大厅接单。

## 三、财务中心（fin）
- **对账中心（reconciliations）**：平台/托管户/站方三方对账。
- **分账报告（profit）**：按规则分账报表。
- **资金账户（ledger）** / **支付流水（payments）** / **费率配置（fee）** / **押金管理（deposits）** / **运营方财务（operator）**：均为列表 + 筛选 + 详情/导出。

## 四、风控中心（risk）
- **风控总览（risk）** / **异常告警（alerts）**：风控指标与告警。
- **保险管理（insurance）**：资产保单。
- **争议仲裁（arbitration）** / **投诉处理（complaints）**：纠纷处理流。

## 五、系统中心（sys）
- **用户管理（users）** / **应用门户（app-portal）** / **角色管理（roles）** / **权限矩阵（permission）** / **菜单权限（menu-permission）** / **系统配置（settings）**：增删改查 + 勾选权限。

---

## ⚠️ 已知不一致（"混乱"来源之一，建议后续修）
1. **菜单权限页与真实导航对不上**：「菜单权限（menu-permission）」页用的是 `mockData.MENU_TREE`（一套旧的、更小的菜单树：工作台/产品管理/商品/任务发布/系统设置），与真实侧边栏 `nav.js` 的五大中心结构**不一致**。你在菜单权限里勾选的项，和实际导航不是同一套。建议把菜单权限页统一到 `NAV`。
2. **已按计划移除但仍留路由**：`settlements`（跨境结算）、`countries`（国家/法域）已按 v2「砍全球化」移出菜单，仅后台路由保留（不影响使用）。

## 本次改动
- 产品模板「**新增 / 编辑 / 删除字段**」已可用：入口在 `设备物联 → 产品模板 → 选中某模板 → 右上角「新增字段」`。字段改动存 localStorage，并联动下游品牌产品规格与设备概览的「模板自定义字段」区块。
- 新增文件 `src/mock/templates.js`，承载模板字段的本地可编辑层（接真实后端后改为后端落库）。
