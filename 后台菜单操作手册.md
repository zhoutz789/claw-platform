# 爪平台 · 后台管理菜单操作手册

> 侧边栏的真实导航由 `web/src/nav.js` 的 `NAV` 驱动，当前共 **9 个一级模块 / 46 个菜单项**，本文按此结构逐菜单说明**用途 + 操作方式**。
> 文中写的 `/xxx` 是页面路径，浏览器地址栏里显示为 `#/xxx`（例如用户管理是 `#/users`）。
> 顶部若有黄色「⚠️ 演示数据模式」提示条，表示当前展示的是内置演示数据（闭环可见），接入真实后端并登录后自动切换为实时数据。

---

## 菜单不见了？先读这一段

2026-08-28 的一次菜单改版，曾把**运营中心、财务中心、风控中心、系统中心**四个模块从侧边栏整体摘掉，同时作废了此前保存在本地的自定义菜单配置。于是用户管理、角色管理（权限组）、权限矩阵、菜单权限、菜单管理、系统配置等入口一起“消失”，看起来像功能被删了。

**实际情况是：页面文件和页面路由自始至终都是完好的，只是导航入口被摘掉了。** 2026-08-29 已把四大中心全部恢复，并把自定义配置找了回来。

现在的行为是：

- **菜单项不会再从代码里被删掉。** 系统内置的菜单是“删不掉”的——你在菜单管理页删掉一个内置项，下次打开它会自动补回来。不想看到它，用「隐藏」开关，效果一样而且更安全。
- **显示 / 隐藏 / 排序 / 改名 / 归类，全部在「系统中心 → 菜单管理」里自己改**，改完点保存立即生效，不用改代码、不用找研发。

**以后再遇到“某个菜单不见了”，先做这三步：**

1. 打开**系统中心 → 菜单管理**，在左侧结构树里搜一下，看它是不是被隐藏了（隐藏的项会变灰并标「隐藏」）。
2. 如果确实被隐藏，选中它 → 关掉「在菜单中隐藏」开关 → 点右上角**保存布局**。
3. 如果整棵树都乱了，点右上角**恢复默认**一键回到系统内置菜单。

---

## 一、菜单自主配置（系统中心 → 菜单管理 `/menu-manager`）

这一页就是给非技术人员用的菜单控制台。侧边栏长什么样，全在这一页决定。

### 能做什么

| 想做的事 | 怎么操作 |
| --- | --- |
| **改名** | 左侧选中节点 → 右侧「显示名」里改 → 保存 |
| **隐藏 / 显示** | 选中节点 → 打开「在菜单中隐藏」开关 → 保存 |
| **调顺序** | 选中节点 → 点节点右侧的「上移 / 下移」 |
| **跨中心归类** | 直接把节点拖到别的分组上（比如把页面从运营中心拖到系统中心） |
| **改跳转路径** | 选中节点 → 右侧「路由路径」里改 → 保存 |
| **换图标** | 选中节点 → 右侧「图标」下拉里挑一个 |
| **新增菜单项** | 右上角「新增菜单项」→ 填名称、路径、挂到哪个分组 |
| **新增分组** | 右上角「新增分组」→ 填名称、选挂在哪一层（也可作为一级菜单） |
| **备份 / 恢复** | 「导出配置」下载一份 JSON；「导入配置」把 JSON 读回来 |
| **一键还原** | 「恢复默认」→ 二次确认后回到系统内置菜单 |

**改完一定要点右上角的「保存布局」才会生效。** 没保存时页面底部会有黄条提醒。

### 配置存在哪里

配置保存在**你这台电脑的浏览器本地**（浏览器 localStorage，键名 `claw_menu_override_v5`）。

- 刷新页面、关掉浏览器再打开，**都还在**。
- **换一台电脑、换一个浏览器、或者清理浏览器数据，配置会丢。**
- 所以重要的菜单方案，请点「导出配置」存一份 JSON 文件，换机器时用「导入配置」读回来。

### 三个容易踩的坑

1. **系统内置的菜单项删不掉**（下次打开会自动补回来）。这不是故障，是刻意设计：保证系统功能不会因为误删而丢失。不想显示就用「隐藏」开关。
2. **删除分组有两条规则**：带页面的菜单项不能删；分组下面还有子菜单时，要先把子菜单移走才能删。能删的只有你自己新建的、且已经清空的分组。
3. **隐藏 ≠ 禁用**。隐藏只是不在菜单里显示，知道地址栏路径的人仍然能直接打开那个页面。要做真正的权限管控，请用**角色管理（权限组）**和**菜单权限**。

> 隐藏之后，侧边栏、顶部搜索框、工作台「快捷入口」里都会同步消失——这三处共用同一套菜单数据。

---

## 二、菜单总览（9 个一级模块 / 46 个菜单项）

顺序与左侧边栏一致。觉得哪个位置不顺手，都可以在菜单管理页拖动调整。

### 1. 工作台（`/workbench`）
- **用途**：平台经营总览——站点数、资产数、订单/收益汇总、待办事项。
- **操作**：进入即看；卡片可按时间/中心筛选。右侧「快捷入口」跟随菜单配置变化，隐藏掉的菜单不会在这里出现。

### 2. 产品管理
- **产品中心（`/product-center`）**：产品目录与档案。操作：列表筛选 → 点卡片看详情。
- **合格证（`/certificate`）**：设备合格证 + 二维码验真（防套牌）。操作：登记/查询合格证、扫码验真。
- **绑定 / 产权（`/bind-ownership`）**：设备与产权关系的绑定与解绑。操作：选设备 → 关联产权主体。
- **产品模板（`/product-template`）**：平台级「类」定义（电动两轮车 / 共享电池 / 植保无人机），含可自定义字段。**已可点「新增字段」扩展建模**（类型：数字/文本/单选/日期/布尔 + 单位 + 示例），改动存本地，自动联动到派生品牌产品与设备概览。同样的建模能力也在「运营中心 → 设备物联 → 产品模板」这个 tab 里。
- **数据接入（`/device-data-access`）**：设备数据接入与上报配置。操作：配置接入参数 → 查看上报状态。
- **赋权管理（`/authorization`）**：设备/资产的授权与权限下发。操作：选资产 → 配置授权对象与有效期。

### 3. 项目管理
- **项目中心（`/project-management`）**：项目台账与进度。操作：新建项目 → 维护成员、阶段与交付物。

### 4. 商品管理
- **商品列表（`/goods-list`）**：在售商品清单。操作：筛选/上下架 → 点行看详情。
- **发布商品向导（`/product-wizard`）**：分步引导发布商品。操作：按向导填资料 → 提交。
- **品牌方入驻（`/brand-onboarding`）**：新品牌方入驻申请与审核。操作：填写资料 → 提交 → 审核。
- **厂家商品（`/manufacturer`）**：品牌方及其商品。操作：查看品牌资质/商标验证、商品清单。
- **订单管理（`/order-manage`）**：商品侧订单。操作：按状态/日期筛选 → 看详情、处理发货与售后。

> 说明：**厂家商品、品牌方入驻、订单管理**这三项，早期版本挂在运营中心下，现在统一归入**商品管理**。如果你记得它们在运营中心，用菜单管理页拖回去即可。

### 5. 任务发布
- **无人机任务（`/task-drone`）**：植保 / 巡检 / 救援任务发布与大厅接单。操作：发布任务 → 查看接单与执行进度。
- **资产出租（`/task-rent`）**：选资产 + 日租金 + 权限模板发布出租/共享任务。操作：选资产 → 定价 → 发布。

### 6. 运营中心
- **订单总览（`/orders`）**：全平台订单列表。操作：按状态/日期筛选 → 点行看详情。
- **换电记录（`/swap-orders`）**：换电订单流水。操作：列表查看 + 导出。
- **站点管理（`/stations`）**：服务站 / 换电站台账。操作：新增/编辑站点、查看站内资产与站长。
- **资产台账（`/assets`）**：全部资产（车 / 电池 / 无人机）列表与状态。操作：筛选、查看资产档案。
- **资产溯源（`/asset-trace`）**：单资产全生命周期轨迹（建档 → 运营 → 维修 → 转让 → 退役）。操作：选资产编号看时间线 + 收益说明。
- **产权链追溯（`/custody`）**：所有权转移链（三专户托管记录）。操作：查某资产历次转让。
- **设备物联（`/product-iot`）** — 核心建模区，4 个 tab：
  - **产品模板**：平台级「类」定义，可「新增字段」扩展建模，改动持久化本地并联动下游。
  - **品牌产品**：品牌方基于模板创建的实例，展示规格 spec + 设备数；点卡片看名下设备。
  - **设备**：每台具体对象；点「数字孪生详情」看定位/赋权/收益/维修/转让；无人机额外有「低空·载荷」（空域合规 / 飞行计划 / 作业计量 / 生命周期闭环）。
  - **合格证**：设备合格证 + 二维码验真（防套牌）。
- **共享池（`/shared-pool`）**：资产共享池入池 / 出池与共享调度。操作：列表 + 调度动作。
- **残值回收（`/recovery`）**：退役资产回收与残值核算（固定 30% 残值归资产所有人）。操作：发起回收 → 核算残值。

> 说明：**资产参数**早期挂在运营中心下，现在归入**系统中心**。

### 7. 财务中心
- **资金账户（`/ledger`）**：账户余额与流水。操作：筛选 → 查看详情。
- **支付流水（`/payments`）**：支付明细。操作：按时间/状态筛选 → 导出。
- **押金管理（`/deposits`）**：押金收取、冻结与退还。操作：查询 → 处理退还。
- **跨境结算（`/settlements`）**：跨境结算记录（各算各的）。操作：列表查看 + 详情。
- **三方对账（`/reconciliations`）**：平台 / 托管户 / 站方三方对账。操作：选周期 → 看差异 → 处理。
- **分账报告（`/profit`）**：按规则分账报表。操作：选时间范围 → 查看/导出。
- **费率配置（`/fee`）**：计价费率规则。操作：配置费率 → 保存（锁版计价）。
- **运营方财务（`/operator`）**：运营方维度的财务数据与风险事件。操作：列表 + 筛选 + 详情。

### 8. 风控中心
- **风控总览（`/risk`）**：风控指标大盘。操作：看指标 → 下钻到明细。
- **异常告警（`/alerts`）**：告警列表与处置。操作：筛选未处置 → 逐条处理。
- **保险管理（`/insurance`）**：资产保单与理赔。操作：录保单 → 发起/核定理赔。
- **争议仲裁（`/arbitration`）**：纠纷仲裁流程。操作：受理 → 取证 → 裁决。
- **投诉处理（`/complaints`）**：客户投诉受理与解决。操作：受理 → 跟进 → 结案。

### 9. 系统中心
- **用户管理（`/users`）**：后台账号的增删改查、状态与部门归属。操作：新增/编辑用户 → 分配角色。
- **应用门户（`/app-portal`）**：各端应用入口与配置。操作：维护应用列表与可见范围。
- **角色管理 / 权限组（`/roles`）**：角色定义与权限勾选。操作：新建角色 → 勾选权限 → 赋给用户。
- **权限矩阵（`/permission`）**：角色 × 权限的矩阵视图，一眼看清谁有什么权限。操作：在矩阵里勾选/取消。
- **菜单权限（`/menu-permission`）**：按角色配置菜单可见范围。操作：选角色 → 勾选菜单 → 保存。
- **菜单管理（`/menu-manager`）**：**侧边栏的自主配置页**，改名/隐藏/排序/归类/新增/导入导出都在这里，详见上文第一章。
- **系统配置（`/settings`）**：系统级参数。操作：填表单 → 保存。
- **国家·法域（`/countries`）**：国家与法域（合规管辖）维护。操作：增删改查 + 启停。
- **资产参数（`/asset-params`）**：产权现值核算参数（年利率 / 计息方式 / 按资产类型差异化）。操作：填表单 → 保存。

---

## 三、已知不一致

1. **菜单权限页与真实导航不是同一套数据**：「菜单权限（`/menu-permission`）」页目前用的是页面内置的 `mockData.MENU_TREE`（一套更旧、更小的菜单树），与侧边栏真实的 9 模块 / 46 项结构**对不上**。在该页勾选的项，和实际导航不是同一套。建议后续把菜单权限页统一到 `nav.js` 的 `NAV` 上。

---

## 本次改动

- **恢复四大中心**：运营中心、财务中心、风控中心、系统中心全部回归侧边栏。系统中心现包含用户管理、应用门户、角色管理（权限组）、权限矩阵、菜单权限、菜单管理、系统配置、国家·法域、资产参数共 9 项。
- **菜单配置不再被清空**：自定义菜单配置改为「增量合并」。系统以后新增菜单项，会自动出现在你的菜单里；你自己的改名、隐藏、排序、路径、图标、自建项一律保留，不再因为系统升级而丢失。
- **菜单管理页增强**：新增菜单项 / 新增分组、编辑跳转路径、自选图标、导出备份、导入恢复、恢复默认二次确认。
- **产品模板「新增 / 编辑 / 删除字段」**已可用：入口在「产品管理 → 产品模板」或「运营中心 → 设备物联 → 产品模板」→ 选中某模板 → 右上角「新增字段」。字段改动存本地，并联动下游品牌产品规格与设备概览的「模板自定义字段」区块。
- 承载模板字段本地可编辑层的文件是 `web/src/mock/templates.js`（接真实后端后改为后端落库）。
