# Claw 平台 · 云部署选型与代码版本切换方案

> 配套可运行文件（已生成，位于本仓库）：
> - `deploy/docker-compose.prod.yml` — 生产编排（infra + backend + web + 网关）
> - `deploy/.env.prod.example` — 环境变量模板（含 `CLAW_VERSION` 版本开关）
> - `deploy/nginx/gateway.conf` — 网关 TLS 终止 + 反向代理
> - `web/Dockerfile` + `web/nginx.conf` — 前端生产镜像
> - `scripts/deploy-version.sh` — 构建 / 切换 / 回滚 / 状态

---

## 0. 本地已验证（本次"运行本地系统"的结论）

本地全栈已跑通，证明代码在真实链路下可用：

| 组件 | 状态 | 验证方式 |
|------|------|---------|
| 基础设施 | PG16/TimescaleDB、Redis、RabbitMQ、EMQX 全部运行 | `docker ps` |
| 后端 | `:8080` 启动成功，Flyway 校验 91 个迁移、schema 停在 v90，无待执行 | `actuator/health` → `{"status":"UP"}` |
| 鉴权链路 | 短信验证码回显 → 登录拿 JWT（`PLATFORM_ADMIN`）→ 带 token 调管理接口 | 全绿 |
| 前端 | `:5173` Vite dev，代理 `/api` → 后端 | HTTP 200 |

**运行即发现并修复一个生产级安全 bug**：原 `SecurityConfig` 把支付回调白名单写成
`/api/v1/payments/**/callback` 与 `/api/v1/payments/**/fail`。
Spring Security 6 的 `PathPatternParser` 要求 `**` 只能出现在路径末尾，该写法在
匹配期抛出 `PatternParseException`，导致**所有落到该匹配器的已认证请求（含 `/admin/**`）
统一返回 500**。已改为单段通配 `*`，本地复测 `/admin/permissions/mine` 由 500 → 200，
并提交 `a1321ab`（仅本地提交，未推送）。

> 教训：安全链路的路径匹配不能用 Ant 风格的"中间 `**`"，这是一类会在"启动不报错、运行时才炸"
> 的隐患。后续新增白名单务必用 `*`（单段）或把 `**` 放在末尾。

---

## 1. 当前部署资产盘点

| 已有 | 缺什么 |
|------|--------|
| `deploy/docker-compose.yml` 仅起 infra（PG/Redis/MQ/EMQX） | **没有把应用本身编排进 compose** |
| `backend/Dockerfile` 多阶段构建后端镜像 | 没有前端生产镜像（本次已补 `web/Dockerfile`） |
| `.gitlab-ci.yml` 仅是 S0 骨架（test → build image → deploy TODO） | 没有可用的生产部署/回滚流程（本次已补 `deploy-version.sh`） |
| `deploy/ssl/` 自签名证书 | 没有 Let's Encrypt / 云证书接入说明 |

结论：从"能在本机 docker 起来"到"可上云版本化部署"，缺口是**应用编排 + 网关 + 版本切换机制**——
本次已补齐。

---

## 2. 云服务器选型

### 2.1 形态对比（柬埔寨金边首站 / MVP 阶段）

| 方案 | 说明 | 优点 | 缺点 | 适用 |
|------|------|------|------|------|
| **A. 单机 Docker Compose** | 一台云服务器装 Docker，跑全部容器（含 PG/Redis/MQ/EMQX/backend/web/nginx） | 最省成本、最简单、运维零门槛 | 单点故障；IO/内存争抢；PG 与业务同机 | **MVP 首选** |
| **B. 单机 + 托管数据库** | 同 A，但 PG/Redis 用云托管（RDS / 云 Redis） | 数据可靠性↑、自动备份、免运维 | 成本略高；跨网络延迟 | 数据敏感、要省心备份时 |
| **C. K8s / 容器服务** | 托管 K8s 跑 Deployment+Service | 弹性、多副本、滚动更新 | 复杂度与成本陡增；MVP 过度设计 | 规模化、多区域后 |
| **D. 多机编排（Swarm/K8s）** | 多节点 | 高可用 | 运维重 | 上量后 |

**推荐：MVP 走 A（单机 Compose），数据库优先选 B 的"托管 PG"**。理由：
- 平台当前是单站 MVP（金边），单台 8C16G 足够承载寄售 + 履约 + 少量 IoT 接入；
- 资金/账本数据最怕丢，PG 用云托管（自动备份 + 时间点恢复）把"数据零丢失"从自运维降级为云保障；
- EMQX（IoT/无人机）内存占用大，若单机吃紧可后续独立一台或上云 MQTT；MVP 先同机跑。

### 2.2 规格建议（单机，方案 A/B）

| 资源 | MVP 起步 | 上量后 |
|------|---------|--------|
| vCPU | 4（最低）→ 8 推荐 | 8–16 |
| 内存 | 8 GB（MySQL/PG 同机偏紧）→ **16 GB 推荐** | 32 GB |
| 系统盘 | 100 GB SSD（系统+镜像） | 200 GB+ |
| 数据盘 | 独立云盘挂 `/var/lib/docker` + PG 数据，便于快照 | 同上、加密 |
| 带宽 | 5 Mbps 起步（金边本地）；IoT 上行另计 | 按量 |

> 内存是头号瓶颈：PG + Redis + RabbitMQ + EMQX + JVM(`-XX:MaxRAMPercentage=75`) 同机，
> 16 GB 是舒适线；8 GB 需关 EMQX 或降 JVM 比例。

### 2.3 域名 / 证书 / 网关
- 域名：`api.<domain>` 与 `<domain>`（前端）；TLS 用 Let's Encrypt（certbot）或云证书。
- 网关：`deploy/nginx/gateway.conf` 已做 443 TLS 终止，`/api` → backend:8080，`/` → web。
- 80 强制 301 跳 443。证书挂到 `deploy/ssl/`（生产替换为 `fullchain.pem`+`privkey.pem`）。

---

## 3. 部署架构（生产）

```
                         ┌─────────────────────────────────────────┐
   用户/App/IoT ──HTTPS──▶│  云服务器 (单机 Docker)                  │
                         │                                         │
                         │   [gateway nginx :443]                  │
                         │      │ /api        │ /                  │
                         │      ▼            ▼                     │
                         │  [backend:8080]  [web nginx :80 SPA]    │
                         │      │                                 │
                         │      ├──▶ [postgres/TimescaleDB]  (建议托管)
                         │      ├──▶ [redis]                      │
                         │      ├──▶ [rabbitmq]                   │
                         │      └──▶ [emqx]  (IoT/无人机, 可选)    │
                         └─────────────────────────────────────────┘
```

- 版本切换只动 `backend` / `web` 两个镜像 tag，网关与 infra 不动 → 变更面小、回滚快。
- 数据库独立于应用版本：Flyway 前向迁移随 backend 启动自动执行。

---

## 4. 代码版本更换（核心诉求）

### 4.1 版本命名与流转

```
git tag v1.2.3  ──▶  CI/手动 build  ──▶  claw-backend:v1.2.3 , claw-web:v1.2.3
                                           │
                                           ▼
                          deploy/.env.prod : CLAW_VERSION=v1.2.3
                                           │
                                           ▼
                  docker compose -f deploy/docker-compose.prod.yml --env-file deploy/.env.prod up -d
```

前后端**共用同一版本号**，保证接口契约对齐（避免"前端 v2 调后端 v1 缺字段"）。

### 4.2 日常切换（三步）

```bash
# 1) 构建并打 tag（本机构建；若用云 registry 先 docker login）
./scripts/deploy-version.sh build v1.2.3

# 2) 切换部署版本
./scripts/deploy-version.sh deploy v1.2.3
#   → 自动改写 deploy/.env.prod 的 CLAW_VERSION、拉取/使用本地镜像、up -d、等健康

# 3) 查看 / 回滚
./scripts/deploy-version.sh current          # 当前 / 上一个版本
./scripts/deploy-version.sh rollback         # 一键回滚到上一个版本
```

### 4.3 回滚与数据库的关键约束（务必遵守）

- **Flyway 只前向**：backend 启动时自动执行新迁移（如 V91）。回滚到旧镜像前，
  **必须先恢复该次升级前的 DB 快照**，否则旧应用可能不兼容已应用的新 schema → 启动失败或数据错乱。
- 因此每次"带迁移的升级"前，先对 PG 做一次快照（云控制台 / `pg_dump`）。
- 纯"无新迁移"的版本切换（如改业务逻辑但不动 schema）可直接回滚，无需 DB 操作。
- 资金链路零容忍：回滚后用旧镜像跑一遍核心核对（账户余额恒等式、待履约订单冻结额），再放开流量。

### 4.4 零停机 / 蓝绿（进阶，非 MVP 必需）
- MVP：`up -d` 会先停旧容器再起新容器，有数秒中断（manageable）。
- 要零停机：在 compose 外保留"蓝/绿"两组 backend+web，nginx `upstream` 切流；或用 K8s 滚动更新（方案 C）。
- 网关与 infra 不参与版本切换，天然免中断。

---

## 5. 上线检查清单（安全收尾）

承接此前安全加固，上云前确认：

1. **`claw.security.dev-open-access` 必须为 `false`**（生产默认已是 false；若误开需同时 `dev-open-access-ack=true` 才放行，护栏会拦）。
2. **三个密钥必须注入**：`CLAW_JWT_SECRET`、`CLAW_ID_CARD_KEY`、`CLAW_PAYMENT_CALLBACK_SECRET`（支付回调 fail-closed，缺则回调 401）。
3. 网关 `/actuator/health` 已 `allow 127.0.0.1; deny all`，不对外暴露。
4. 数据库用托管 + 自动备份；密钥走云密钥管理，不进 `.env.prod` 明文提交（已默认 gitignore）。
5. TLS 启用 HSTS/强套件（gateway.conf 已置 TLS1.2/1.3）。
6. 上线前跑通：短信登录 → 下单冻结 → 取货履约结算 → 回调入账 全链路；并对"无注解写端点"按此前清单逐批补 `@RequirePermission`（高危两项已修）。

---

## 6. 一句话总结

> **MVP 上云 = 一台 8C16G 云服务器 + Docker Compose（PG 优先托管）+ nginx 网关；
> 版本切换 = `git tag` → 镜像 tag → 改 `CLAW_VERSION` → `deploy-version.sh deploy/rollback`；
> 回滚前先备 DB 快照。本次已把缺失的"应用编排 + 网关 + 切换脚本"全部补齐并验证 compose 语法通过。**
