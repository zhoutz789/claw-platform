# 前端模块 1 完成小结 · 布局滚动 + 旧菜单清理（2026-09-01）

> 按「单块确认、逐块推进」节奏执行。本模块为 5 项需求中的第 1 块，做完请用户在浏览器确认后再进模块 2。

## 改动清单

### 1. 布局滚动修复 · `web/src/layout/AdminLayout.jsx`
原问题：最外层 `minHeight:100vh`、Sider 的 `Menu` 与 `Content` 均无独立滚动容器 → 整页一起滚动，菜单跟着内容下滑。

4 处修改：
- 最外层 `<Layout>`：`minHeight:'100vh'` → `height:'100vh', overflow:'hidden'`（锁死整页，禁止整体滚动）
- `<Sider>`：追加 `overflow:'auto'`（侧边栏菜单独立滚动）
- 右侧 `<Layout>`：追加 `style={{ height:'100vh', display:'flex', flexDirection:'column' }}`（Header 固定、Content 撑满）
- `<Content>`：`flex:1, height:0, overflow:'auto'`（右侧内容区独立滚动条）

效果：菜单固定、内容区从上到下独立滚动，不再联动下滑。

### 2. 删除旧菜单 · `web/src/nav.js`
「商品管理」(`goods`) 子树下删除 4 个旧页面入口（用户确认不适用）：
- `manufacturer`（厂家商品）
- `brand-onboarding`（品牌方入驻）
- `order-manage`（订单管理）
- `merchants`（商家入驻）

保留：`goods-list`（商品列表）、`product-wizard`（商品向导/发布）。
（注：仅移除菜单入口，对应页面文件/路由保留，避免误删其它引用；后续若需彻底清理可再删文件。）

## 验证
- `npm run build`：✓ 3281 modules transformed，3.96s，零错误
- dev server http://localhost:5173：http=200，HMR 已热更新
- 后端 http://localhost:8080：仍运行（H2 零安装模式，作为模块 1 验证底座）

## 用户确认项
请刷新浏览器 http://localhost:5173 验证：
1. 点击不同菜单时，左侧菜单栏不再跟随右侧内容下滑；右侧内容区有独立滚动条。
2. 「商品管理」下只剩「商品列表」「商品向导」两项，旧 4 项已消失。

## 下一步（模块 2，待确认）
真实数据库环境：当前后端为 H2 内存库（数据重启即空）。用户要求"以后都给真实体验环境"。
需启动 Docker 中间件（PG/Redis/RabbitMQ/EMQX）并切默认 profile 连真实库 + 注入权限种子（管理员可操作全部数据、角色按权限自动显隐菜单）。
Docker 当前未运行，需确认启动方式（我尝试 `open -a Docker` / 用户手动 / 暂用 H2 先验证逻辑）。
